%examles of or 
%(or (clause clause ... ))
%examples of clause
%((rule rule ...)(outcome))
%examples of rule
%```(= (+ x y) z)``` They check if two things can be set equal.
%outcome can be a variable ```x```, or it could be another `or` tree, or it could be an `end(a,b,c)` function.

